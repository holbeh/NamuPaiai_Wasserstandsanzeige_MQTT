{
    "tasks": [
        {
            "type": "shell",
            "label": "clang++ build active file",
            "command": "/home/kloenk/.nix-profile/bin/clang++",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/home/kloenk/.nix-profile/bin"
            }
        }
    ],
    "version": "2.0.0"
}